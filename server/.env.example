# Colyseus server environment variables
# Copy to .env and fill in values (never commit .env)

# Port for the Colyseus WebSocket server
PORT=2567

# Allowed CORS origin (Next.js dev server)
CLIENT_ORIGIN=http://localhost:3000
