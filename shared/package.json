{
    "name": "@runebound-tactics/shared",
    "version": "1.0.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean" : "tsc --build --clean",
        "build": "tsc --build",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src",
        "test": "jest"
    },
    "peerDependencies": {
        "@colyseus/schema": "^4.x"
    },
    "devDependencies": {
        "@colyseus/schema": "^4.0.0",
        "@types/jest": "^29.5.12",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.5",
        "typescript": "^5.8.3"
    }
}
