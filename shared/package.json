{
    "name": "@runebound-tactics/shared",
    "version": "1.0.0",
    "private": true,
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc --build",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src"
    },
    "peerDependencies": {
        "@colyseus/schema": "^2.x"
    },
    "devDependencies": {
        "@colyseus/schema": "^2.0.0",
        "typescript": "^5.8.3"
    }
}
